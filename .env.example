ANTHROPIC_API_KEY=sk-ant-...
DATABASE_PATH=./data/pages.db
PORT=8000
HOST=0.0.0.0

# Security: set a secret to require Bearer token auth on /api/generate
# Leave empty to allow unauthenticated access (not recommended for public servers)
API_SECRET=

# Rate limiting for /api/generate (per IP)
RATE_LIMIT_REQUESTS=10
RATE_LIMIT_WINDOW=60
